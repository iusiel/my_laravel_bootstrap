describe("basic spec", () => {
    it("page loads", () => {
        cy.visit("/");
    });

    it("text exists on page", () => {
        cy.findByText("Some text that should be seen on the page.");
    });
});
